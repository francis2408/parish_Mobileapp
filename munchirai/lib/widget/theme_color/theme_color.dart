import 'package:flutter/material.dart';

// Home screen menu section
const menuPrimaryColor = Color(0xFF2C96D2);
const menuSecondaryColor = Color(0xFF5EE6EB);
const menuThirdColor = Color(0xFFFFC68F);
const menuFourthColor = Color(0xFF8DDAFF);
const menuIconColor = Colors.white;
const menuTextColor = Colors.black;

const backgroundColor = menuPrimaryColor;
const screenBackgroundColor = Color(0xFFe1e5f2);
const appBackgroundColor = menuPrimaryColor;
const screenColor = Color(0xFFFFFFFF);

// Splash Screen
const first = menuPrimaryColor;
const second = menuSecondaryColor;

// Appbar color
const primaryColor = Color(0xFF1A2F83);
const secondaryColor = menuSecondaryColor;

// Authentication screen
const darkColor = menuPrimaryColor;
const lightColor = menuSecondaryColor;

// Icon button with icon color
const iconBackColor = menuPrimaryColor;
const buttonIconColor = whiteColor;
const noDataButtonColor = menuPrimaryColor;

// Tabbar Color
const tabBackColor = menuPrimaryColor;
const tabLabelColor = whiteColor;
const unselectColor = blackColor;

// Text field color
const noDataColor = Color(0xFFE6A519);
const textColor = Colors.blueAccent;
const textHeadColor = Color(0xFF7B66FF);
const iconColor = Color(0xFF2D479A);
const expandColor = Color(0xFF2D479A);
const disableColor = Color(0xFFF0F0F1);
const enableColor = Color(0xFF00A3D3);
const inputColor = Colors.white;
const inputColors = Color(0xFFF0F0F0);
const greenColor = Colors.green;
const blackColor = Colors.black;

// Navigation bar color
const navIconColor = Color(0xFF00A3D3);
const iconActiveColor = Color(0xFF00A3D3);
const accountShadow = Color(0xFF00A3D3);
const containerShadow = menuPrimaryColor;
const navTextColor = Colors.black;
const navBackgroundColor = Color(0xFFFFEDE9);
const redColor = Colors.red;
const orangeColor = Colors.orange;
const purpleColor = Colors.deepPurple;
const whiteColor = Colors.white;

// Button Background
const buttonGreen = Colors.green;
const buttonRed = Color(0xFFF44336);
const buttonLabel = Colors.white;

// Mobile / Phone icon color
const mobileText = Color(0xFF56BBF1);
const emailText = Colors.redAccent;
const callColor = Colors.blueAccent;
const smsColor = Colors.orangeAccent;
const whatsAppColor = Colors.green;
const emailColor = Colors.blueAccent;

// Active and Non-Active color
const nonActiveColor = Colors.red;
const activeColor = Colors.green;

// Status Completed and Active color
const statusCompleted = Colors.teal;
const statusActive = Colors.green;
const statusTextColor = Colors.white;

// Text label and value color
const labelColor = Colors.black87;
const labelColor2 = Colors.black54;
const valueColor = Colors.black;
const emptyColor = Colors.grey;
const countLabel = Colors.black;
const countValue = Colors.black;
const hiLightColor = Color(0xFF2D479A);

// for news, other
const customTextColor1 = orangeColor;
const customTextColor2 = Colors.blueAccent;
const customBackgroundColor1 = Color(0xFFFFF4EE);
const customBackgroundColor2 = Color(0xFFD1E6FF);
const customGradientColor1 = backgroundColor;
const customGradientColor2 = backgroundColor;
const customGradientColor3 = backgroundColor;